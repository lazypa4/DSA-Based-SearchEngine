<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ParamEngine</title>
    <style>
        /* General Reset */
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            display: flex;
            flex-direction: column;
            min-height: 100vh;
            background-color: #fff;
            color: #202124;
        }

        /* Main Content Area */
        main {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            flex-grow: 1;
            margin-top: -80px;
        }

        /* Logo Styling */
        .logo {
            font-size: 80px;
            font-weight: bold;
            letter-spacing: -2px;
            margin-bottom: 25px;
            user-select: none;
        }

        .logo span:nth-child(1) { color: #4285f4; }
        .logo span:nth-child(2) { color: #ea4335; }
        .logo span:nth-child(3) { color: #fbbc05; }
        .logo span:nth-child(4) { color: #4285f4; }
        .logo span:nth-child(5) { color: #34a853; }
        .logo span:nth-child(6) { color: #000000; } /* E - Black */

        /* Search Bar Container */
        .search-container {
            width: 100%;
            max-width: 584px;
            padding: 0 20px;
        }

        .search-bar {
            display: flex;
            align-items: center;
            width: 100%;
            height: 46px;
            border: 1px solid #dfe1e5;
            border-radius: 24px;
            padding: 0 15px; /* Padding for the container */
            margin: 0 auto;
            transition: all 0.3s ease;
        }

        .search-bar:hover, .search-bar:focus-within {
            background-color: #fff;
            box-shadow: 0 1px 6px rgba(32,33,36,0.28);
            border-color: rgba(223,225,229,0);
        }

        /* Input Field */
        input[type="text"] {
            flex-grow: 1;
            border: none;
            outline: none;
            font-size: 16px;
            background-color: transparent;
            height: 100%;
            margin-right: 10px; /* Space between text and button */
            margin-left: 5px;   /* Space from left edge */
        }

        /* Search Button (Icon) styling */
        .search-btn {
            background: none;
            border: none;
            cursor: pointer;
            padding: 5px;
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 50%;
            transition: background 0.2s;
        }

        /* Search Icon SVG */
        .search-icon {
            width: 24px;
            height: 24px;
            fill: #4285f4; /* Changed to Blue to make it look like an active button */
        }

        .search-btn:hover {
            background-color: #f1f3f4; /* Subtle circle background on hover */
        }
    </style>
</head>
<body>

    <main>
        <div class="logo">
            <span>P</span><span>a</span><span>r</span><span>a</span><span>m</span><span>E</span><span>n</span><span>g</span><span>i</span><span>n</span><span>e</span>
        </div>

        <div class="search-container">
            <form action="#" method="GET">
                <div class="search-bar">
                    <input type="text" name="q" autocomplete="off" autofocus placeholder="Search DSA Based Problems">
                    
                    <button type="submit" class="search-btn" aria-label="Search">
                        <svg class="search-icon" viewBox="0 0 24 24">
                            <path d="M15.5 14h-.79l-.28-.27A6.471 6.471 0 0 0 16 9.5 6.5 6.5 0 1 0 9.5 16c1.61 0 3.09-.59 4.23-1.57l.27.28v.79l5 4.99L20.49 19l-4.99-5zm-6 0C7.01 14 5 11.99 5 9.5S7.01 5 9.5 5 14 7.01 14 9.5 11.99 14 9.5 14z"/>
                        </svg>
                    </button>
                </div>
            </form>
        </div>
    </main>

</body>
</html>