<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Results - ParamEngine</title>
    <style>
        :root {
            --easy: #00af9b;
            --medium: #ffb800;
            --hard: #ff2d55;
            --google-blue: #4285f4;
            --google-red: #ea4335;
            --google-yellow: #fbbc05;
            --google-green: #34a853;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background-color: #f8f9fa;
            color: #202124;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        /* Header Styling */
        header {
            background: #fff;
            padding: 15px 5%;
            border-bottom: 1px solid #dfe1e5;
            display: flex;
            align-items: center;
            gap: 30px;
            position: sticky;
            top: 0;
            z-index: 100;
        }

        /* Logo Styling */
        .mini-logo {
            font-size: 24px;
            font-weight: bold;
            text-decoration: none;
            letter-spacing: -1px;
            display: flex;
            user-select: none;
        }

        /* Color "Param" */
        .mini-logo span:nth-child(1) { color: var(--google-blue); }
        .mini-logo span:nth-child(2) { color: var(--google-red); }
        .mini-logo span:nth-child(3) { color: var(--google-yellow); }
        .mini-logo span:nth-child(4) { color: var(--google-blue); }
        .mini-logo span:nth-child(5) { color: var(--google-green); }
        
        /* Color "Engine" as Black */
        .mini-logo .engine { color: #000000; }

        .header-search-container {
            flex-grow: 1;
            max-width: 692px;
            display: flex;
            align-items: center;
            border: 1px solid #dfe1e5;
            border-radius: 24px;
            padding: 5px 15px;
        }

        .header-search-container input {
            flex-grow: 1;
            border: none;
            outline: none;
            font-size: 16px;
            padding: 5px;
        }

        /* Layout */
        .container {
            display: flex;
            padding: 20px 5%;
            gap: 40px;
        }

        aside { width: 200px; flex-shrink: 0; }

        .filter-section { margin-bottom: 30px; }
        .filter-section h3 {
            font-size: 14px;
            color: #70757a;
            text-transform: uppercase;
            margin-bottom: 15px;
        }

        .filter-option {
            display: flex;
            align-items: center;
            margin-bottom: 12px;
            font-size: 14px;
            cursor: pointer;
        }

        .filter-option input { margin-right: 12px; }

        /* Result Cards */
        .results-main { flex-grow: 1; max-width: 650px; }
        
        .result-card {
            background: #fff;
            padding: 18px;
            border-radius: 8px;
            border: 1px solid #dfe1e5;
            margin-bottom: 16px;
        }

        .result-title {
            font-size: 19px;
            color: #1a0dab;
            font-weight: 500;
            margin-bottom: 8px;
            display: block;
        }

        .result-meta {
            display: flex;
            align-items: center;
            gap: 12px;
            margin-top: 10px;
        }

        .badge {
            font-size: 11px;
            font-weight: bold;
            padding: 2px 10px;
            border-radius: 12px;
            color: #fff;
            text-transform: uppercase;
        }

        .badge.easy { background-color: var(--easy); }
        .badge.medium { background-color: var(--medium); }
        .badge.hard { background-color: var(--hard); }

        .tags { display: flex; gap: 6px; flex-wrap: wrap; }
        .tag {
            background: #f1f3f4;
            color: #3c4043;
            font-size: 12px;
            padding: 2px 8px;
            border-radius: 4px;
        }
    </style>
</head>
<body>

    <header>
        <div class="mini-logo">
            <span>P</span><span>a</span><span>r</span><span>a</span><span>m</span><span class="engine">Engine</span>
        </div>
        
        <div class="header-search-container">
            <input type="text" placeholder="Search problems...">
        </div>
    </header>

    <div class="container">
        <aside>
            <div class="filter-section">
                <h3>Difficulty</h3>
                <label class="filter-option"><input type="checkbox"> Easy</label>
                <label class="filter-option"><input type="checkbox"> Medium</label>
                <label class="filter-option"><input type="checkbox"> Hard</label>
            </div>
            <div class="filter-section">
                <h3>Language</h3>
                <label class="filter-option"><input type="checkbox"> C++</label>
                <label class="filter-option"><input type="checkbox"> Java</label>
                <label class="filter-option"><input type="checkbox"> Python</label>
            </div>
        </aside>

        <main class="results-main">
            <div class="result-card">
                <div class="result-title">Problem Title Placeholder</div>
                <div class="result-meta">
                    <span class="badge medium">Difficulty</span>
                    <div class="tags">
                        <span class="tag">Tag 1</span>
                        <span class="tag">Tag 2</span>
                    </div>
                </div>
            </div>
        </main>
    </div>

</body>
</html>